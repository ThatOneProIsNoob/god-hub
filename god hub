@@ -1,7 +1,7 @@

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local StarterGui = game:GetService("StarterGui")

-- Function to create the ThatOnePro GUI from scratch
-- Function to show the ThatOnePro GUI
local function showThatOneProGui()
    local player = Players.LocalPlayer
    if not player then
@@ -20,21 +20,147 @@

        existingGui:Destroy() -- Remove it if it already exists
    end
    
    -- Create the ScreenGui from scratch
    local screenGui = Instance.new("ScreenGui")
    screenGui.Name = "ThatOnePro"
    screenGui.IgnoreGuiInset = true
    screenGui.Parent = playerGui
    
    -- Create main frame
    local mainFrame = Instance.new("Frame")
    mainFrame.Name = "MainFrame"
    mainFrame.Size = UDim2.new(0.5, 0, 0.5, 0)
    mainFrame.Position = UDim2.new(0.25, 0, 0.25, 0)
    mainFrame.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
    mainFrame.BorderSizePixel = 0
    mainFrame.Parent = screenGui
    
    -- Create title label
    local titleLabel = Instance.new("TextLabel")
    titleLabel.Name = "TitleLabel"
    titleLabel.Size = UDim2.new(1, 0, 0.1, 0)
    titleLabel.Position = UDim2.new(0, 0, 0, 0)
    titleLabel.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    titleLabel.BorderSizePixel = 0
    titleLabel.Text = "ThatOnePro Panel"
    titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    titleLabel.TextSize = 24
    titleLabel.Font = Enum.Font.SourceSansBold
    titleLabel.Parent = mainFrame
    
    -- Create scrolling frame for content
    local scrollingFrame = Instance.new("ScrollingFrame")
    scrollingFrame.Name = "ContentScrollFrame"
    scrollingFrame.Size = UDim2.new(0.9, 0, 0.7, 0)
    scrollingFrame.Position = UDim2.new(0.05, 0, 0.15, 0)
    scrollingFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    scrollingFrame.BorderSizePixel = 0
    scrollingFrame.ScrollBarThickness = 6
    scrollingFrame.CanvasSize = UDim2.new(0, 0, 2, 0) -- Allow scrolling
    scrollingFrame.Parent = mainFrame
    
    -- Create text content in scrolling frame
    local contentText = Instance.new("TextLabel")
    contentText.Name = "ContentText"
    contentText.Size = UDim2.new(0.95, 0, 0.95, 0)
    contentText.Position = UDim2.new(0.025, 0, 0.025, 0)
    contentText.BackgroundTransparency = 1
    contentText.Text = "This is the ThatOnePro Panel. Use this interface to access various features and functionality."
    contentText.TextColor3 = Color3.fromRGB(255, 255, 255)
    contentText.TextSize = 16
    contentText.Font = Enum.Font.SourceSans
    contentText.TextWrapped = true
    contentText.TextXAlignment = Enum.TextXAlignment.Left
    contentText.TextYAlignment = Enum.TextYAlignment.Top
    contentText.Parent = scrollingFrame
    
    -- Create unban button
    local unbanButton = Instance.new("TextButton")
    unbanButton.Name = "UnbanButton"
    unbanButton.Size = UDim2.new(0.4, 0, 0.08, 0)
    unbanButton.Position = UDim2.new(0.3, 0, 0.9, 0)
    unbanButton.BackgroundColor3 = Color3.fromRGB(59, 165, 93)
    unbanButton.BorderSizePixel = 0
    unbanButton.Text = "Unban Request"
    unbanButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    unbanButton.TextSize = 18
    unbanButton.Font = Enum.Font.SourceSansBold
    unbanButton.Parent = mainFrame
    
    -- Create unban remote event if it doesn't exist
    local unbanRemote = ReplicatedStorage:FindFirstChild("UnbanRequest")
    if not unbanRemote then
        unbanRemote = Instance.new("RemoteEvent")
        unbanRemote.Name = "UnbanRequest"
        unbanRemote.Parent = ReplicatedStorage
    -- Get the original GUI from StarterGui
    local originalGui = StarterGui:FindFirstChild("ThatOnePro")
    if not originalGui then
        warn("ThatOnePro ScreenGui not found in StarterGui")
        return
    end
    
    -- Add unban button functionality
    local unbanButtonScript = Instance.new("LocalScript")
    unbanButtonScript.Parent = unbanButton
    unbanButtonScript.Source = [[
local button = script.Parent
local unbanRemote = game:GetService("ReplicatedStorage"):FindFirstChild("UnbanRequest")
local players = game:GetService("Players")
local localPlayer = players.LocalPlayer
local screenGui = button.Parent.Parent

button.MouseButton1Click:Connect(function()
    -- When the button is clicked, fire the remote event to request an unban
    if unbanRemote then
        unbanRemote:FireServer()
        button.Text = "Request Sent!"
        button.BackgroundColor3 = Color3.fromRGB(100, 100, 100)
        task.wait(2)
        button.Text = "Unban Request"
        button.BackgroundColor3 = Color3.fromRGB(59, 165, 93)
    else
        button.Text = "Error: Remote not found"
        button.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
        task.wait(2)
        button.Text = "Unban Request"
        button.BackgroundColor3 = Color3.fromRGB(59, 165, 93)
    end
end)

-- Listen for server response
if unbanRemote then
    unbanRemote.OnClientEvent:Connect(function(success)
        if not success then
            -- If unban failed, show a notification
            button.Text = "Unban Failed"
            button.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
            task.wait(2)
            button.Text = "Unban Request"
            button.BackgroundColor3 = Color3.fromRGB(59, 165, 93)
        end
    end)
end
]]
    -- Clone the GUI with all its original elements and positions
    local guiClone = originalGui:Clone()
    guiClone.Parent = playerGui
    
    -- Create close button
    local closeButton = Instance.new("TextButton")
    closeButton.Name = "CloseButton"
    closeButton.Size = UDim2.new(0.05, 0, 0.05, 0)
    closeButton.Position = UDim2.new(0.95, -5, 0, 5)
    closeButton.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
    closeButton.BorderSizePixel = 0
    closeButton.Text = "X"
    closeButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    closeButton.TextSize = 14
    closeButton.Font = Enum.Font.SourceSansBold
    closeButton.Parent = mainFrame
    -- Make sure it's visible
    guiClone.Enabled = true
    
    -- Add close button functionality
    local closeScript = Instance.new("LocalScript")
    closeScript.Parent = closeButton
    closeScript.Source = [[
script.Parent.MouseButton1Click:Connect(function()
    script.Parent.Parent.Parent:Destroy()
end)
]]
    
    print("ThatOnePro GUI has been created and displayed")
    print("ThatOnePro GUI has been displayed with all elements in their original positions")
end

-- Execute immediately when the script runs

